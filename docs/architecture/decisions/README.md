# Architecture Decision Records

This folder holds Architecture Decision Records (ADRs) for ApprovalIQ, one Markdown file per decision (e.g. `0001-use-nestjs-for-api.md`).

Decisions will be recorded here as the platform is designed and built.
