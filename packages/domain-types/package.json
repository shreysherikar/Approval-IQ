{
  "name": "@approvaliq/domain-types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared domain types â€” BusinessProfile, Approval, Document, Authority, etc.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
    "typescript": "^5.6.2"
  }
}
