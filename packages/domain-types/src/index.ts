/**
 * @approvaliq/domain-types — shared domain model types
 * (BusinessProfile, Approval, Document, Authority, ...).
 *
 * Intentionally empty for the scaffold phase. The next prompts will add the
 * domain model definitions here.
 */
export {};
