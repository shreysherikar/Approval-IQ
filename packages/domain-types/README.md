# @approvaliq/domain-types

Shared domain model types for ApprovalIQ: `BusinessProfile`, `Approval`, `Document`, `Authority`, and related entities. Pure TypeScript — no runtime dependencies.

Consumed by: `packages/contracts`, `services/*`, `apps/web`, `packages/test-fixtures`.
