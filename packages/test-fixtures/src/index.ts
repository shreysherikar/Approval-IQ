/**
 * @approvaliq/test-fixtures — shared fixture data for tests
 * across services and apps.
 *
 * Intentionally empty for the scaffold phase. Fixture data will be added as
 * the services are implemented.
 */
export {};
