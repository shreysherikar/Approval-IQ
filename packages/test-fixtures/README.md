# @approvaliq/test-fixtures

Shared test fixture data (sample business profiles, approvals, documents, authorities). Used by tests in `services/*` and `apps/web` so every workspace tests against the same data.
