{
  "name": "@approvaliq/storage",
  "version": "0.0.1",
  "private": true,
  "description": "ApprovalIQ storage provider abstraction — local filesystem adapter + S3 stub",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test test/local-storage-provider.test.ts",
    "test:types": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "eslint": "^9.10.0",
    "typescript": "^5.6.2"
  }
}