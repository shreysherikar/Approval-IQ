# @approvaliq/contracts

Shared TypeScript types and Zod schemas for the API DTOs. Both `services/api` and `apps/web` import from this package so request/response shapes stay in sync.

Consumed by: `services/api`, `apps/web`.
