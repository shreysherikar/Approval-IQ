{
  "name": "@approvaliq/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "Shared API DTO contracts â€” TypeScript types + Zod schemas",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@approvaliq/domain-types": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
    "typescript": "^5.6.2"
  }
}
