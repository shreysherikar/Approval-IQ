/**
 * @approvaliq/approval-engine — pure approval evaluation logic.
 *
 * CONSTRAINT: this package must stay pure TypeScript. No NestJS, database,
 * or HTTP imports are allowed here so the engine remains portable and
 * trivially unit-testable.
 *
 * Intentionally empty for the scaffold phase. The engine implementation
 * will be added in the next prompts.
 */
export {};
