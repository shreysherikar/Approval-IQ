{
  "name": "@approvaliq/document-engine",
  "version": "0.0.1",
  "private": true,
  "description": "ApprovalIQ document engine — extraction providers (Phase 6)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test test/*.test.ts",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@approvaliq/domain-types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "eslint": "^9.10.0",
    "typescript": "^5.6.2"
  }
}
